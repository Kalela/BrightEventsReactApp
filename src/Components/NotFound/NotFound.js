import React from 'react';

import Navbar from '../Navbar/Navbar';

const NotFound = () => (
  <div>
    <Navbar />
    <h1> Oops! We could not find that page. (404) </h1>
  </div>
);

export default NotFound;
